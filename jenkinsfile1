pipeline {
	agent { label 'master' }
	stages {
		stage('Build') {
		    steps {
		    	sh 'echo this is my first stage in pipeline'
			sh 'ls -lrt'
			}
		}
	
		stage('Test') {
			steps {
				sh 'echo this is my first stage in pipeline'
				sh 'du -sh'
			}
		}
	}
}
